/// Web 平台桩实现：PDF 文本提取暂不支持
Future<String?> extractFromFile(String filePath, {int maxChars = 20000}) async {
  return null;
}

Future<String?> extractFromBytes(List<int> bytes,
    {int maxChars = 20000}) async {
  return null;
}
